# Deployment secrets and runtime state (ADR-0009 §7,§8)
.env
.data/

# Terraform (infra/terraform)
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
terraform.tfvars
secrets.auto.tfvars

# LaTeX build artifacts (docs/whitepaper) — keep .tex source and .pdf output
*.aux
*.fdb_latexmk
*.fls
*.log
*.out
*.toc
*.xdv
